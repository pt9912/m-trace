# Review-Report: welle-02 (Harness-Regelwerk-Migration v3.5.1 → v6.8.0) — 2026-09-13

**Review-Art:** Plan — geprüft wird der Wellenplan gegen Regelwerk-Templates,
`AGENTS.md` §3 und `harness/conventions.md` (Modul 10 §Drei Review-Arten).
Vor der Implementation der noch offenen Tranchen 5–7, 9–11.

**Gegenstand:**
`docs/plan/planning/welle-02-regelwerk-v6.8.0-migration.md` (Stand
`ec58962`), plus die bereits umgesetzte Tranche 8
(`docs/plan/planning/done/slice-016-roadmap-offene-wellen-format.md`) und
die vier geschnittenen, noch nicht umgesetzten Slices
`docs/plan/planning/open/slice-01{3,4,5}*.md`
(Tranche 2–4) sowie `slice-012` (Tranche 1).

**Skill:** `.harness/skills/reviewer.md` @ `Accepted` (v3.5.1-Fassung, keine
Versions-Historie im Repo) · <!-- d-check:ignore (Adopter-spezifischer Skill-Pfad) -->
**Modell:** `claude-sonnet-5` · **Datum:** 2026-09-13

**Eingangs-Kontext** (die Verträge, gegen die geprüft wurde — ohne diese
Liste ist der Lauf nicht reproduzierbar):

- `welle-02-regelwerk-v6.8.0-migration.md` (Plan-Dokument, Tranchen-Tabelle,
  §8 Owner-Entscheidungen)
- direkt verifizierte Regelwerk-Quellen: `.harness/baseline/v3.5.1/regelwerk/`
  (Bestand) gegen die SHA256-verifizierten `v6.8.0`-Release-Assets
  (`lab-regelwerk.zip`/`SHA256SUMS`), insb. `modul-03-spec.md`,
  `modul-04-adrs.md`, `roadmap.template.md`
- `AGENTS.md` §3 (Harte Regeln), insb. §3.3 (`git mv` + Inhaltsänderung),
  §3.6 (Gate-Senkung ohne ADR), §3.7 (Variante-B-Disziplin)
- `harness/conventions.md` (MR-001..MR-007, aktueller Adaptions-Bestand)
- `scripts/lint-variante-b.py` (Scope-Definition, gegen die §3.7 geprüft
  wurde)
- vorherige Findings am gleichen Modul: keine (erster Review-Report zu
  einer `welle-02`-Tranche)

---

## Findings

Jedes Finding folgt dem **§Output-Schema des Reviewer-Skills** — der
verbindlichen Single Source of Truth. Die Felder unten sind nur gespiegelt,
nicht neu definiert.

### F-1 — Tranche 11 fehlte in der Sequenzbegründung (§7)

- `kategorie`: LOW
- `quelle`: Maintainability (interne Konsistenz eines Planungsdokuments)
- `pfad`: `docs/plan/planning/welle-02-regelwerk-v6.8.0-migration.md` §7
  (vor Commit `ec58962`)
- `befund`: Tranche 11 (Review-Harness-Templates) wurde nachträglich zu §4
  (Tranchen-Tabelle) und §5 (Abhängigkeiten) hinzugefügt, aber nicht in die
  §7-Sequenzbegründung eingearbeitet — die Prosa endete bei „T9/T10
  zuletzt", ohne T11 zu erwähnen. Kein Entscheidungsfehler, nur eine
  Lücke in der Dokument-Vollständigkeit. **Während dieses Reviews behoben**
  (Commit `ec58962`, ein Satz ergänzt).
- `verifizierbar`: ja — `grep -n "T11\|Tranche 11" welle-02-*.md` zeigt die
  Ergänzung in §7 seit `ec58962`.

## Negativbefunde

Eine Zeile pro betrachtetem Bereich. Ohne diesen Block ist „keine
Findings" nicht von „nicht geprüft" unterscheidbar.

- geprüft, ohne Befund: **Faktentreue der Kernbehauptung** (Architektur
  wandert von Modul 4 zu Modul 3) — direkt gegen die extrahierten
  `v6.8.0`-Regelwerk-Dateien verifiziert: `modul-03-spec.md` trägt den
  Titel „Die Spec: Lastenheft, Spezifikation, Architektur",
  `modul-04-adrs.md` nur noch „ADRs". Deckt sich mit der Welle-Aussage.
- geprüft, ohne Befund: **Hard Rule 3.3** (`git mv` + Inhaltsänderung = zwei
  Commits) — alle Lifecycle-Moves dieser Session
  (`slice-010`/`011`/`016`, je `open→in-progress→done`) einzeln per
  `git show --stat` gegengeprüft: jeder Move-Commit zeigt `0 insertions(+),
  0 deletions(-)`, reiner Rename.
- geprüft, ohne Befund: **Hard Rule 3.6** (keine Gate-Senkung ohne ADR) —
  alle Änderungen dieser Session (`planning.waves`, `links.resolve-from`,
  `mode: many`, `heading`-Override, `ignore-refs`-Ergänzung) sind additiv
  oder schärfer, keine gesenkte Prüfung.
- geprüft, ohne Befund: **Hard Rule 3.7 / Variante-B-Scope** — Verdacht
  geprüft, ob `roadmap.md`s „(Welle-Datei §8)"-Verweis und `welle-02`s
  durchgängige „Tranche N"/„§N"-Selbstreferenzen einen Verstoß darstellen.
  Entkräftet: `make lint-variante-b` scannt laut Aufruf in `Makefile`
  (`apps/api apps/dashboard apps/analyzer-service packages examples
  scripts spec docs/user docs/dev .github/workflows Makefile`) —
  `docs/plan/planning/` ist **nicht** im Scope. Die Hard Rule zielt auf
  Spec-/Releasing-/Makefile-Dokumente als Zielbild; Planungsartefakte, deren
  Wesen gerade die Tranchen-/Wellen-Referenz ist, sind davon ausdrücklich
  ausgenommen (siehe `feedback_variante_b_spec`-Konvention).
- geprüft, ohne Befund: **`hostpaths`-Konformität** — `grep -n
  "/Development\|/Users\|/home/"` über `welle-02*.md` und alle vier neuen
  `open/`-Slices liefert keinen Treffer; der Hinweis auf das
  maschinenlokale Referenz-Repo (§„Ausgangsrecherche") ist bewusst
  generisch gehalten, kein absoluter Pfad im committeten Text.
- geprüft, ohne Befund: **§8-Owner-Entscheidungen** — alle vier
  (Spec-Straten-Form, Roadmap-Terminologie, Review-Report-Archivierung,
  ADR-Trigger-Grandfathering) sind als echte, unentschiedene Fragen markiert
  und nicht stillschweigend vorentschieden; Punkt 2 (Roadmap-Terminologie)
  korrekt als „entschieden, Mechanik bereits umgesetzt" aktualisiert, nach
  der tatsächlichen Owner-Entscheidung.
- geprüft, ohne Befund: **Abhängigkeits-Konsistenz (§5)** — Tranche 7 →
  Tranche 11 (`Klasse`-Spalte braucht Beobachtungs-Register) und Tranche 2 →
  Tranche 10 (`vcs`-Sensor braucht MR-Einzeldateien) sind in §5 **und** in
  den einzelnen Slice-Texten (`slice-013`§4) konsistent wiederholt, kein
  Widerspruch gefunden.
- geprüft, ohne Befund: **MR-001-Sonderfall in `slice-013`** — die
  Diskrepanz „Template erwartet Ablösung durch Nachfolger-MR, `MR-001` wurde
  aber durch Slice-Arbeit aufgelöst" ist als offene Implementierungsfrage
  markiert, nicht als (falsche) Tatsachenbehauptung verkauft.
- geprüft, ohne Befund: **`make gates`/`make docs-check`-Nachweis** — jeder
  Slice-Abschluss dieser Session trägt einen dokumentierten Positiv- **und**
  Negativ-Probe-Lauf (`slice-010`: `wave-drift` reproduziert;
  `slice-011`: `link-position-dependent` reproduziert; `slice-016`:
  0-Befund-Bestätigung nach Marker-Rücksetzung), keine unbelegte
  Grün-Behauptung.

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 1 |
| INFO | 0 |

## Verdikt

**Merge-blockierend:** nein — die einzige Findung (F-1) ist LOW und wurde
im selben Zug behoben. Der Wellenplan ist intern konsistent, seine
Kernbehauptungen sind gegen die tatsächlichen Regelwerk-Quellen verifiziert
(nicht nur aus Fork-Zusammenfassungen übernommen), und die vier bisherigen
Lifecycle-Übergänge halten Hard Rule 3.3 sauber ein.

**Übergabe:** Findings gehen an die Implementation der noch offenen
Tranchen (5, 6, 7, 9, 10, 11). Der Report ersetzt keine Verifikation —
DoD-/Spec-Konformität der einzelnen Tranchen-Umsetzungen prüft der
Verifier separat bei ihrem jeweiligen Abschluss (Modul 11).
