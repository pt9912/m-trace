# Review-Report: slice-004 (Driving-Port-Boundary) — 2026-07-24

**Review-Art:** Code — geprüft wird der fertige Diff gegen Plan +
Konventionen (Modul 10 §Drei Review-Arten). Retro-Review eines bereits
gemergten Slice als **erster echter Modul-8/10-Handoff-Report** (slice-007:
Review-Report-Praxis).

**Gegenstand:** Commit `bd7e3a8` — „refactor(api): slice-004 —
Driving-Port-Boundary säubern (Handler ↛ application)". 16 Files
(Go + `.a-check.yml` + Slice-Doc-Move).

**Skill:** `.harness/skills/reviewer.md` @ `7894e44` · <!-- d-check:ignore (Adopter-spezifischer Skill-Pfad) -->
**Modell:** `claude-opus-4-8[1m]` · **Datum:** 2026-07-24

**Eingangs-Kontext** (die Verträge, gegen die geprüft wurde — ohne
diese Liste ist der Lauf nicht reproduzierbar):

- Slice-Plan `docs/plan/planning/done/slice-004-driving-port-boundary.md`
  (Ziel, DoD, 3-Cluster-Plan A/B/C)
- `spec/architecture.md` §3.3 (rollenbasierte Ports) und §3.4 (die vier
  Layer-Regeln), Referenzstratum View
- `.a-check.yml` (deklarative Layer-Regeln; slice-003 löste
  `check-architecture.sh` ab)
- `AGENTS.md` §3 (Harte Regeln), insb. §3.3 (`git mv` + Inhaltsänderung),
  §3.4 (Architektur), §3.5 (ADR-Immutabilität), §3.6 (Gate-Senkung)
- vorherige Findings am gleichen Modul: keine (erster Report in
  `docs/reviews/` der Handoff-Klasse)

---

## Findings

Jedes Finding folgt dem **§Output-Schema des Reviewer-Skills** — der
verbindlichen Single Source of Truth. Die Felder unten sind nur
gespiegelt, nicht neu definiert.

### F-1 — a-check-Boundary greift nur für Produktivcode

- `kategorie`: INFO
- `quelle`: Maintainability (`.a-check.yml`-Scope)
- `pfad`: `.a-check.yml:9` (`exclude: apps/api/**/*_test.go`)
- `befund`: a-check prunt `_test.go` aus dem Scan (geerbt von
  `check-architecture.sh`). Acht Test-Files unter
  `adapters/driving/http/` importieren weiter `hexagon/application`
  (`handler_test.go`, `router_test.go`, `sse_stream_test.go` u. a.). Die
  DoD hat „nicht-Test" bewusst ausgeklammert, daher kein Verstoß — aber
  eine künftige Boundary-Erosion **auf der Test-Seite** würde vom Gate
  nicht gefangen.
- `verifizierbar`: teilweise — `grep -rl hexagon/application
  apps/api/adapters/driving/http/*_test.go` zeigt die acht Files;
  `make arch-check` bestätigt es **nicht** (Test-Exclusion).

### F-2 — „Wire-Format"-Frame liegt in `domain`

- `kategorie`: INFO
- `quelle`: Maintainability (Layer-Reinheit)
- `pfad`: `apps/api/hexagon/domain/event_stream.go:3`
- `befund`: `EventAppendedFrame` wird im Doc-Kommentar als
  „Mindest-Wire-Format für SSE-Live-Updates" bezeichnet und liegt in
  `domain`; der HTTP-Adapter (`sse_stream.go:writeAppendedFrame`)
  marshalt den Typ direkt über einen separaten `frameWire`. Der Typ ist
  als Domänen-Event vertretbar; die „Wire-Format"-Bezeichnung in der
  Domänenschicht ist die einzige beobachtbare Layer-Unschärfe des Diffs.
- `verifizierbar`: nein — Konventions-Beurteilung, kein Gate-Signal.

## Negativbefunde

Eine Zeile pro betrachtetem Bereich. Ohne diesen Block ist „keine
Findings" nicht von „nicht geprüft" unterscheidbar.

- geprüft, ohne Befund: `hexagon/domain/` (`errors.go`,
  `event_stream.go`) — `ErrAnalyzeManifestEmpty`/`ErrSrtHealthStreamUnknown`
  und `EventAppendedFrame` sauber verschoben, Sentinel-Werte + Feldsatz
  identisch, keine Semantikänderung.
- geprüft, ohne Befund: `hexagon/application/` — Ports via
  Compile-Time-Assertion erfüllt (`var _ driving.EventStreamInbound =
  (*EventBroker)(nil)`, `var _ driving.SrtHealthInbound =
  (*SrtHealthQueryService)(nil)`); Fehler-Rückgaben auf `domain.Err…`
  umgestellt; Rückgabetypen von `application.*` auf `driving.*`
  umgezogen, Signatur-Semantik unverändert.
- geprüft, ohne Befund: `hexagon/port/driving/`
  (`event_stream_inbound.go`, `srt_health_inbound.go`) — beide **im
  selben Commit** definiert (Verdacht „Split-Commit ⇒ nicht
  kompilierbar / Hard Rule 3.3" per `git ls-tree bd7e3a8` **geprüft und
  entkräftet**: die `--stat`-Ausgabe war zunächst nur abgeschnitten).
- geprüft, ohne Befund: `adapters/driving/http/` (Produktivcode) —
  **kein** `hexagon/application`-Import mehr (`grep` über Nicht-Test-Files
  leer); Fehler-Mapping `errors.Is(err, domain.Err…)` korrekt
  nachgezogen (`analyze.go`, `srt_health_handlers.go`); SSE-Frame- und
  SRT-Wire-Encoding unverändert; die lokal definierte `SrtHealthInbound`
  aus dem Adapter entfernt (war selbst die zu tilgende Kopplung).
- geprüft, ohne Befund: `cmd/api/main.go` — Broker und SrtHealth als
  Driving-Port (`driving.SrtHealthInbound`) durchgereicht; korrekter Ort
  (Composition-Root, `.a-check.yml:composition_root`).
- geprüft, ohne Befund: `.a-check.yml` — genau die eine
  `adapters→application`-Kante entfernt; keine Regel-Aufweichung sonst;
  Gate laut Commit + DoD grün (Hard Rule 3.6 nicht berührt).
- geprüft, ohne Befund: Hard Rules §3 — kein `git mv` + Inhaltsänderung
  an derselben Datei im selben Commit (die Typ-„Moves" sind
  Löschung-hier/Neuanlage-dort im Code, keine `git mv`-Renames); keine
  Änderung an Accepted-ADRs; keine Inline-Suppression.

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 0 |
| INFO | 2 |

## Verdikt

**Merge-blockierend:** nein — keine HIGH/MEDIUM. Sauberer,
semantik-erhaltender Refactor; die Driving-Port-Boundary hält für
Produktivcode und ist ab diesem Commit maschinell (a-check) durchgesetzt.
Die zwei INFO-Findings sind Hinweise ohne erwartete Aktion (F-1 als
möglicher Folge-Gate-Kandidat, wenn Test-seitige Erosion je auftritt).

**Übergabe:** Findings gehen an die Implementation. Der Report ersetzt
keine Verifikation — DoD-/Spec-Konformität prüft der Verifier separat
(Modul 11). Da der Slice bereits in `done/` liegt, ist dieser Report ein
**Retro-Exemplar**: er belegt das Format an echtem Code, nicht einen
offenen Merge-Block.
